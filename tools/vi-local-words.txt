# Local / dialect words to include in the Vietnamese glide dictionary.
#
# One word per line. Optionally add a zipf100 weight after whitespace:
#
#   rứa           <- weight auto-derived: max(wordfreq value, 450)
#   mệ 520        <- explicit weight (wins over auto)
#
# '#' starts a comment. Words are lowercased and NFC-normalized automatically;
# the swipe key sequence (rứa -> rua) is derived automatically too.
# These entries OVERRIDE the wordfreq-generated entry for the same word.
#
# After editing, run:  python3 tools/generate_gesture_dictionary.py vi
# then check with:     ./gradlew test
#
# Weight guide: ~500+ everyday, ~450 common in speech, ~250 niche. Words sharing
# a key path rank purely by this number and only the top 3 fit the suggestion
# strip — see the note in generate_gesture_dictionary.py before raising values.

rứa           # "vậy/thế" — #2 on the "rua" path
dồ            # crowded "do" path (đó/độ/do/đồ...); needs ~630 to reach the strip
mệ 520        # "bà" — #2 on the "me" path
choa          # "chúng tôi"
